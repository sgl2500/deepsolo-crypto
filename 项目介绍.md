这个项目主要是完成基于原始 K 线数据的选币预览，并且沉淀选币逻辑、信号和买点复盘的一套本地工具。

平台设计必须遵循桌面端高密度数据分析界面的思路：表格优先、筛选高效、状态清晰、适合长期复盘。

数字货币的数据来源在运行时通过 `DATA_ROOT` / `CRYPTO_DATA_ROOT` 配置。数据目录应符合 `docs/data-format.md` 里的 `normalized_gzip` 结构。真实数据不进入 Git，开源使用者可以用自己的数据路径，也可以先用 `scripts/generate-sample-data.py` 生成样例数据体验。


现在我希望这个平台有几个功能

大的功能模块

1.选币查询，主体就是合约纬度表，可以通过不同的指标+公式，选出不同的币种合约。还带有时间，可以切换时间查询（参考功能参考图，选币查询.png）注意我们可以讨论细节功能
2.指标生产，这块的功能就是可以基于数据源的基础数据来编写公式，从而定义一些预计算的指标定义，单独定义
3.指标仓库，这块功能就是将生产的指标，固定化成一个指标，可以被选币查询复用
4.数据源管理，这里存储不同的外部数据源，提供一些简单的预览功能，主要是外部接口的一些查询预览和查看的一些定义。
